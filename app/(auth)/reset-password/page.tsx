//app/(auth)/reset-password/page.tsx
import ResetPasswordForm from './ResetPasswordForm'

export const metadata = { title: 'Set a new password — EarnGro' }

export default function ResetPasswordPage() {
  return <ResetPasswordForm />
}